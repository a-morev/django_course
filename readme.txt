Сделал 3 позиции товаров для каталога и увеличил количество товаров в них.
При существующей разметке элементы поиск и корзина пришлось добавлять словами и спец. символами.

Дальше по заданиям 6 урока:
1 - ok;
2 - Не удается просмотреть содержимое корзины, получаю страницу, что корзина пуста, хотя в БД товары есть и в
в главном меню сумма покупки отображается. Ничего не понимаю, что произошло, ошибки ни какие не выскакивают
надеюсь на Вашу помощь.
3 -  При реализации просмотра карточки товара не удается скорректировать адрес в catalog.html,
на данный момент реализовал рендер страниц задавая явно pk=1, чтобы просмотреть, что рендериться хоть. Также меня
смущает, как реализовал в category.html, хотя все работает здесь. Прокомментируйте, пожалуйста, в этой строке почему
в одном случае pk=product.pk,  а во втором явно задавать пришлось, только после этого исчезла ошибка:
        <a href="{% url 'main:product_page' pk=product.pk %}">
            <figure><img src="/media/{{ product.image }}" width="300" height="200" alt="">
                <figcaption><a href="{% url 'main:product_page' pk=1 %}">{{ product.name }}</a></figcaption>
            </figure>
        </a>
4 - ok;

Поскольку провозился с обозначенными выше проблемами не успел сделать верстку для "горячего предложения", чтобы
проработать его. И реализацию active в меню переношу на следующий раз.
